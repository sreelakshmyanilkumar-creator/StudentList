#ifndef SORT_H
#define SORT_H

#include <stdio.h>
#include <string.h>
#include "student.h"
#include "linkedlist.h"
#include "common.h"

bool SortByName(Node* pstlinkedlistHead);
bool SortByRoll(Node* pstlinkedlistHead);

#endif // SORT_H